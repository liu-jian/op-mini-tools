#脚本用途与目的
    1. 删除指定目录，指定天数之前的hdfs数据，并且不可回收。

#脚本结构
.
├── bin
│   ├── run.sh      #启动脚本
│   └── utils.sh    #公共函数
├── etc
│   └── vars.sh     #配置文件
└── log             #日志目录

#脚本使用方式
    1. 填写配置文件
        #示例
        path01='10 /XXX/NNN'

        注意事项：
            a. "path01"前缀名字不能重复
            b. 第一个参数是天数，表示删除多少天之前的数据，第二个参数是删除的路径。
    2. 在任意路径执行run.sh
